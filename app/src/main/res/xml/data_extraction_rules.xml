<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Include alarms and settings in cloud backup -->
        <include domain="database" path="." />
        <include domain="sharedpref" path="." />
    </cloud-backup>

    <device-transfer>
        <!-- Include everything in device transfer -->
        <include domain="database" path="." />
        <include domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
